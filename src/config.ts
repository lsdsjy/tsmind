import { Vector } from './model'

export const config = {
  verticalSpan: 20,
  horizontalSpan: 20,
  textHorizontalPadding: 10,
  textVerticalPadding: 5,
  summaryPadding: 8,
  connectOffset: 5,
  canvasSize: [600, 600] as Vector,
  fill: 'transparent',
  stroke: 'black',
}
