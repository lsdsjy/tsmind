import { Vector } from '../model'

export function measureText(text: string): Vector {
  return [30, 20]
}

function measureTree(): Vector {
  return [0, 0]
}

function measureMindMap(): Vector {
  return [0, 0]
}
